model:
  name: microsoft/deberta-v3-small
  dropout: 0.1
  freeze_encoder_layers: 6  # freeze all encoder layers; only train the classification head

training:
  learning_rate: 1e-3
  batch_size: 16
  epochs: 20
  warmup_ratio: 0.1
  weight_decay: 0.01
  max_length: 512
  use_wandb: false

data:
  train_path: data/processed/train
  val_path: data/processed/val

output:
  dir: outputs/classifier
